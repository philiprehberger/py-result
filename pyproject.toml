[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "philiprehberger-result"
version = "0.2.5"
description = "Rust-inspired Result type for Python with pattern matching and type-safe error handling"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "Philip Rehberger" }]
keywords = ["result", "either", "error-handling", "type-safe", "monad"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Typing :: Typed",
]

[project.urls]
Homepage = "https://github.com/philiprehberger/py-result#readme"
Repository = "https://github.com/philiprehberger/py-result"
Issues = "https://github.com/philiprehberger/py-result/issues"
Changelog = "https://github.com/philiprehberger/py-result/blob/main/CHANGELOG.md"

[tool.hatch.build.targets.wheel]
packages = ["src/philiprehberger_result"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.mypy]
strict = true
